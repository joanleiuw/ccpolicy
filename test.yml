policies:
  - name: my-first-policy
    resource: ec2
    mode: 
      role: arn:aws:iam::047283044064:role/CCustodianLambda 
      type: periodic
      schedule: 'cron(0/1 * * * ? *)'
    filters:
      - "tag:Custodian": present
    actions:
      - stop
